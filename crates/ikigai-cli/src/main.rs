//! ikigai — resource-resolution REPL.
//!
//! Attaches to a kernel instance over a pluggable transport (embedded, shared-memory
//! IPC, or QUIC/HTTP3+mTLS). Placeholder entry point.

fn main() {
    println!("ikigai {}", env!("CARGO_PKG_VERSION"));
    println!("resource-resolution REPL — scaffold, not yet implemented");
}
